# environments & secrets
.env
.venv/
venv/

# private runtime data (never commit, never serve)
runtime/

# python
__pycache__/
*.pyc
.pytest_cache/

# node / tooling
node_modules/

# OS noise
.DS_Store
Thumbs.db

# Google service-account credentials. The real file belongs outside the repo
# entirely (GOOGLE_APPLICATION_CREDENTIALS points at it); this is a second
# line of defence against one landing here by accident.
*service-account*.json
ga4-*.json
